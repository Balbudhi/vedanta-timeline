# Wave 1 Job 1.1 — Primitive-graph §0 insertion

## Output summary

- Worktree file edited: `data/articles/source/primitive-graph.md`
- Total new lines inserted in that file: `353`
- Exact `§0` line range: `3-334`
- Rule additions in `§2`: `Rule 7` at line `473`, `Rule 8` at line `480`
- File line-count after edit: `1932`

## Audit checkpoint against `PHASE0_INTERNALIZATION.md` T1-T14

Verdict: `Yes, with one explicit preservation of a source-side count mismatch rather than a fabricated harmonization.`

- `T1` is satisfied. The new `§0.1`, `§0.3`, and `§0.7` insist that thinkers must be read on their own terms and under their own `adhikāra` and `viṣaya`.
- `T2` is satisfied. `§0.1` and `§0.7` make the double rule explicit: false disagreements dissolve; genuine disagreements remain.
- `T3` is satisfied in compressed form. `§0.3` and `§0.7` distinguish the seeker-world from the realized-world through scope and addressee.
- `T4` is satisfied in compressed form. `§0.7 Case 1A` places Vallabha on the realized-ontological side and Śaṅkara's `mithyā` claim on the seeker-side.
- `T5` is satisfied in principle. `§0.7` preserves Vallabha's constructive ontology rather than flattening it into a mere pedagogical variant.
- `T6` is partially deferred by design. The hymn-register rule is stated in `AF5`, but no specific Śaṅkara hymn is identified here because the brief for this job did not require a new hymn-identification pass and the exact verse remains unstable in the methodology papers.
- `T7` is satisfied at the methodological level. `§0.4` and `AF9` preserve the distinction between phenomenological, metaphysical, and logical-dialectical burdens instead of collapsing them.
- `T8` is satisfied. `§0.4` anchors the discipline in Patañjali and Bhartṛhari, with the Bhartṛhari `vāc` numbering marked `[textually-not-fully-confirmed]` where the OCR witness is unstable.
- `T9` is satisfied. `§0.6` copies and applies `AF1-AF9`, and `§0.7` shows one dissolved and one preserved disagreement.
- `T10` is satisfied. `§0.2` and `§2 Rule 4` install the working register taxonomy and the finer optional layer.
- `T11` is satisfied indirectly. `§0.2` and `§0.5` provide the register and language-game vocabulary needed for later KCB handling without forcing KCB content into this front section.
- `T12` is satisfied indirectly. The graph now states the discipline that later glossary and popover rewrites are meant to implement.
- `T13` is satisfied directly. The primitive graph now has the requested `§0`, plus `Rule 7` and `Rule 8`.
- `T14` is satisfied as a front-end condition. The section is written as corpus-wide method, not as a one-off Śaṅkara-Vallabha exception.

## Notes

- The methodology papers themselves are numerically inconsistent on the register total. `SCOPE_REGISTER_FRAMEWORK.md §1` explicitly names sixteen registers, while nearby planning prose speaks of seventeen. The new `§0.2` preserves that mismatch rather than inventing an unattested twelfth `P1` label.
- The Bhartṛhari `vāc` hierarchy is included as a `P1` escalation rule, not as a universal first-pass tool, following `PLAN_v2.md §3`.
